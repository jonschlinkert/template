'use strict';

var App = require('..');
var app = new App();


app.iterator('sync', function (stack) {
  // todo...
});

app.iterator('async', function (stack) {
  // todo...
});

app.iterator('promise', function (stack) {
  // todo...
});

app.iterator('stream', function (stack) {
  // todo...
});
